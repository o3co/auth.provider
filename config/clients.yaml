# NOTE: Override secrets per deployment. These placeholders MUST NOT be used in production.
test-app:
  clientSecret: "CHANGE_ME_CLIENT_SECRET"
  allowedRedirectUris:
    - "http://localhost:3000/callback"
  allowedScopes:
    - "read"
    - "write"
