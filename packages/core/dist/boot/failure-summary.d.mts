/** `Name: message`; `Name` when the rules keep no message; `a thrown <kind>` for a non-Error. */
export declare function failureSummary(thrown: unknown): string;
/** The message alone by the same rules, or `""` when they keep none. */
export declare function failureDetail(thrown: unknown): string;
//# sourceMappingURL=failure-summary.d.mts.map