services:
  test:
    build:
      context: .
      target: test
    depends_on:
      redis:
        condition: service_healthy

  redis:
    image: redis:7.2-alpine
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
