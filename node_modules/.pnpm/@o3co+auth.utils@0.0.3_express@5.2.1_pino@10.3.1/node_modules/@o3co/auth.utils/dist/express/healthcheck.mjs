import express from "express";
export function createHealthcheckRouter(path = "/healthcheck") {
    const router = express.Router();
    router.get(path, (_req, res) => {
        res.status(200).json({ status: "ok" });
    });
    return router;
}
