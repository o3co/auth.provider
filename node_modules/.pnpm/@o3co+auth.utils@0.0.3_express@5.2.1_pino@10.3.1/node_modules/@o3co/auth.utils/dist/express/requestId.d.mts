import type { RequestHandler } from "express";
export interface RequestIdOptions {
    header?: string;
    generator?: () => string;
}
export declare function createRequestIdMiddleware(options?: RequestIdOptions): RequestHandler;
//# sourceMappingURL=requestId.d.mts.map