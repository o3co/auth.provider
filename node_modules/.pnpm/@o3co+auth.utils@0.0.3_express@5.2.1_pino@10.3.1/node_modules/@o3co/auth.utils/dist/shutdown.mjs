export function gracefulShutdown(server, cleanup) {
    const handler = () => {
        cleanup?.();
        server.close(() => process.exit(0));
    };
    process.on("SIGTERM", handler);
    process.on("SIGINT", handler);
}
